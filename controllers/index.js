
const { Chef, Comment, Ingredient, Picture, Step, Recipe } = require('../models')


module.exports = {
    createChef,
    createComment,
    addIngredient,
    addPicture,
    addStep,
    newRecipe,
    getRecipe,
    getUser,
    getAllComments,
    getAllRecipes,
    
}